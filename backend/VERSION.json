{
    "version": "1.0.2",
    "build_date": "2025-12-13T23:54:00+05:30",
    "commit": "auto-populated-on-deploy",
    "release_notes": "Critical fix: User lookup now handles both MongoDB ObjectId and UUID formats",
    "features": [
        "Authentication (login, register, token refresh)",
        "Patient management (CRUD)",
        "Clinical notes",
        "Sync (pull/push)",
        "User profile management",
        "Beta features (known issues)",
        "Version tracking API"
    ],
    "fixes": [
        "CRITICAL: Fixed user lookup to handle both _id (ObjectId) and id (UUID) fields",
        "Fixed User import in security.py for sync operations",
        "Fixed profile update 500 error",
        "Added retry logic with exponential backoff",
        "Added database indexes for performance"
    ]
}