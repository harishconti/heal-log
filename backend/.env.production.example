# .env.production.example
# -------------------------
# This file contains a template for the environment variables required
# for a production deployment. Copy this file to '.env.production' and
# fill in the values for your environment.

# --- Core Settings ---
# Generate a strong, unique secret key for your production environment.
# You can generate one using: openssl rand -hex 32
SECRET_KEY=your_production_secret_key_here

# The algorithm used for JWT encoding. HS256 is the default.
ALGORITHM=HS256

# The lifetime of the access token in minutes.
ACCESS_TOKEN_EXPIRE_MINUTES=30

# The lifetime of the refresh token in days.
REFRESH_TOKEN_EXPIRE_DAYS=7

# Set the environment to 'production'. This will disable debug features.
ENV=production


# --- Database Settings ---
# Your production MongoDB connection string.
MONGO_URL=mongodb+srv://<user>:<password>@<your-cluster-url>/<your-db-name>?retryWrites=true&w=majority
DB_NAME=your_production_db_name


# --- Cache Settings ---
# Your production Redis connection string.
REDIS_URL=redis://your-production-redis-host:6379


# --- CORS Settings ---
# SECURITY: A comma-separated list of allowed origins for CORS.
# IMPORTANT: NEVER use "*" in production! The application will refuse to start.
# You MUST specify your exact frontend domain(s) here.
ALLOWED_ORIGINS=https://your-frontend.com,https://www.your-frontend.com

# --- Stripe Webhook Settings ---
# Your Stripe webhook signing secret (starts with 'whsec_')
# Get this from your Stripe Dashboard -> Webhooks -> Signing secret
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret_here


# --- Sentry Monitoring ---
# The DSN for your Sentry project.
SENTRY_DSN=your_sentry_dsn_here

# The environment for Sentry (e.g., production).
SENTRY_ENVIRONMENT=production
